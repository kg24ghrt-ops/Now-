pub struct Session {
    // fields...
}

impl Session {
    pub fn new() -> Self { Self {} }
    pub fn feed_text(&mut self, text: &str) {
        // segment, shape, etc.
    }
    pub fn render(&self) {
        // render
    }
}