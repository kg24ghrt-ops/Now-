[package]
name = "hw-script-latin"
version = "0.1.0"
edition = "2021"
description = "Latin script pack for the handwriting engine (fallback baseline)."
license = "MIT OR Apache-2.0"

[dependencies]
hw-core = { workspace = true }
hw-segment = { workspace = true }
hw-shape = { workspace = true }